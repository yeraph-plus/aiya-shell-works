# Runtime minimum (CLI-only, headless Linux friendly)
PyYAML>=6.0,<7.0

# Optional: native GUI
PySide6>=6.9,<7.0  ; platform_system == "Windows" or platform_system == "Linux" or platform_system == "Darwin"

# Optional: native PTY backend on Windows (Linux uses stdlib pty)
pywinpty>=2.0  ; platform_system == "Windows"

# Test
pytest>=8.0,<9.0
pytest-cov>=5.0,<6.0

# Lint
ruff>=0.8,<1.0